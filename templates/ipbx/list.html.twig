{#
  Newmanagement - Lista de IPBXs On-Premise
#}
{% extends 'layout/page.html.twig' %}

{% block content %}
<div class="container-fluid">

  <div class="d-flex justify-content-between align-items-center mb-3">
    <h2>{{ 'IPBXs'|__('newmanagement') }}</h2>
    <a href="{{ path('GlpiPlugin\\Newmanagement\\Ipbx') }}?action=add"
       class="btn btn-primary">
      <i class="ti ti-plus"></i>
      {{ 'Adicionar IPBX'|__('newmanagement') }}
    </a>
  </div>

  <div class="card">
    <div class="table-responsive">
      <table class="table table-hover mb-0">
        <thead>
          <tr>
            <th>{{ 'ID'|__('newmanagement') }}</th>
            <th>{{ 'Nome'|__('newmanagement') }}</th>
            <th>{{ 'Empresa'|__('newmanagement') }}</th>
            <th>{{ 'IP'|__('newmanagement') }}</th>
            <th>{{ 'Versão Asterisk'|__('newmanagement') }}</th>
            <th>{{ 'Ramais'|__('newmanagement') }}</th>
          </tr>
        </thead>
        <tbody>
          {% for item in items %}
          <tr>
            <td>{{ item.id }}</td>
            <td>
              <a href="{{ path('GlpiPlugin\\Newmanagement\\Ipbx') }}?id={{ item.id }}">
                {{ item.name }}
              </a>
            </td>
            <td>{{ item.company_name ?? '-' }}</td>
            <td>{{ item.ip_address ?? '-' }}</td>
            <td>{{ item.asterisk_version ?? '-' }}</td>
            <td>{{ item.extensions_count ?? 0 }}</td>
          </tr>
          {% else %}
          <tr>
            <td colspan="6" class="text-center text-muted py-4">
              {{ 'Nenhum IPBX cadastrado.'|__('newmanagement') }}
            </td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
  </div>

</div>
{% endblock %}
