{#
  Partial: Tabela de Ramais com paginação server-side.
#}

{# CORRIGIDO: max(a,b) é função nativa do Twig — não depende de filtro #}
{% set ext_pages = ext_page_size > 0 ? max(1, (ext_total / ext_page_size)|round(0, 'ceil')) : 1 %}

<div id="nm-ext-section"
     data-section="extensions"
     data-ipbx-id="{{ ipbx_id }}"
     data-companies-id="{{ companies_id }}"
     data-paginate-url="{{ paginate_url|e('html_attr') }}"
     data-action-url="{{ action_url|e('html_attr') }}"
     data-page="{{ ext_page }}"
     data-page-size="{{ ext_page_size }}"
     data-total="{{ ext_total }}">

  <table class="tab_cadre_fixe">
    <thead>
      <tr>
        <th>{{ __('Ramal', 'newmanagement') }}</th>
        <th>{{ __('Senha', 'newmanagement') }}</th>
        <th>{{ __('IP Dispositivo', 'newmanagement') }}</th>
        <th>{{ __('Usuário', 'newmanagement') }}</th>
        <th>{{ __('Grava', 'newmanagement') }}</th>
        <th>{{ __('Departamento', 'newmanagement') }}</th>
        <th></th>
      </tr>
    </thead>
    <tbody id="nm-ext-tbody">
      {% for row in extensions %}
        <tr class="tab_bg_1" id="nm-ext-row-{{ row.id }}">
          <td>{{ row.number }}</td>
          <td>••••••</td>
          <td>{{ row.device_ip }}</td>
          <td>{{ row.user_name }}</td>
          <td>{{ row.records_calls ? __('Sim', 'newmanagement') : __('Não', 'newmanagement') }}</td>
          <td>{{ row.department }}</td>
          <td>
            {% if can_delete %}
            <button type="button"
                    class="btn btn-sm btn-icon nm-del-btn"
                    data-action="delete_extension"
                    data-id="{{ row.id }}"
                    data-row="nm-ext-row-{{ row.id }}"
                    data-companies-id="{{ companies_id }}"
                    data-csrf="{{ csrf|e('html_attr') }}"
                    data-url="{{ action_url|e('html_attr') }}"
                    data-confirm="{{ __('Remover ramal?', 'newmanagement') }}"
                    title="{{ __('Remover', 'newmanagement') }}">
              <i class="ti ti-trash text-danger"></i>
            </button>
            {% endif %}
          </td>
        </tr>
      {% else %}
        <tr id="nm-ext-empty">
          <td colspan="7" class="text-center text-muted py-3">
            {{ __('Nenhum ramal cadastrado.', 'newmanagement') }}
          </td>
        </tr>
      {% endfor %}
    </tbody>
  </table>

  <div id="nm-ext-pagination"
       class="d-flex align-items-center justify-content-between mt-2"
       {% if ext_pages <= 1 %}style="display:none!important"{% endif %}>

    <span class="text-muted" style="font-size:0.8rem">
      {{ __('Mostrando', 'newmanagement') }}
      {{ ((ext_page - 1) * ext_page_size) + 1 }}–{{ min(ext_page * ext_page_size, ext_total) }}
      {{ __('de', 'newmanagement') }} {{ ext_total }}
      {{ __('ramais', 'newmanagement') }}
    </span>

    <div class="d-flex gap-1">
      <button type="button"
              class="btn btn-sm btn-outline-secondary nm-page-btn"
              data-section-id="nm-ext-section"
              data-dir="prev"
              {% if ext_page <= 1 %}disabled{% endif %}
              title="{{ __('Página anterior', 'newmanagement') }}">
        <i class="ti ti-chevron-left"></i>
      </button>

      <span class="btn btn-sm disabled px-3" style="min-width:90px;text-align:center">
        {{ ext_page }} / {{ ext_pages }}
      </span>

      <button type="button"
              class="btn btn-sm btn-outline-secondary nm-page-btn"
              data-section-id="nm-ext-section"
              data-dir="next"
              {% if ext_page >= ext_pages %}disabled{% endif %}
              title="{{ __('Próxima página', 'newmanagement') }}">
        <i class="ti ti-chevron-right"></i>
      </button>
    </div>
  </div>

  {% if can_write and ipbx_id > 0 %}
  <div class="nm-add-form mt-3" id="nm-ext-add-form">
    <table class="tab_cadre_fixe">
      <tr class="tab_bg_2">
        <td><input type="text"     class="form-control" id="nm-ext-number"      placeholder="{{ __('Ramal', 'newmanagement') }}"></td>
        <td><input type="password" class="form-control" id="nm-ext-password"    placeholder="{{ __('Senha', 'newmanagement') }}" autocomplete="new-password"></td>
        <td><input type="text"     class="form-control" id="nm-ext-device_ip"   placeholder="{{ __('IP Dispositivo', 'newmanagement') }}"></td>
        <td><input type="text"     class="form-control" id="nm-ext-user_name"   placeholder="{{ __('Usuário', 'newmanagement') }}"></td>
        <td>
          <select class="form-select" id="nm-ext-records_calls">
            <option value="0">{{ __('Não grava', 'newmanagement') }}</option>
            <option value="1">{{ __('Grava', 'newmanagement') }}</option>
          </select>
        </td>
        <td><input type="text"     class="form-control" id="nm-ext-department"  placeholder="{{ __('Departamento', 'newmanagement') }}"></td>
        <td>
          <button type="button" class="btn btn-sm btn-success nm-add-ext-btn"
                  data-ipbx-id="{{ ipbx_id }}"
                  data-companies-id="{{ companies_id }}"
                  title="{{ __('Adicionar ramal', 'newmanagement') }}">
            <i class="ti ti-plus"></i>
          </button>
        </td>
      </tr>
    </table>
  </div>
  {% endif %}

</div>
