{#
  Newmanagement - Lista de IPBXs Cloud
#}
{% extends 'layout/page.html.twig' %}

{% block content %}
<div class="container-fluid">

  <div class="d-flex justify-content-between align-items-center mb-3">
    <h2>{{ 'IPBXs Cloud'|__('newmanagement') }}</h2>
    <a href="{{ path('GlpiPlugin\\Newmanagement\\IpbxCloud') }}?action=add"
       class="btn btn-primary">
      <i class="ti ti-plus"></i>
      {{ 'Adicionar IPBX Cloud'|__('newmanagement') }}
    </a>
  </div>

  <div class="card">
    <div class="table-responsive">
      <table class="table table-hover mb-0">
        <thead>
          <tr>
            <th>{{ 'ID'|__('newmanagement') }}</th>
            <th>{{ 'Nome'|__('newmanagement') }}</th>
            <th>{{ 'Empresa'|__('newmanagement') }}</th>
            <th>{{ 'Provedor'|__('newmanagement') }}</th>
            <th>{{ 'Região'|__('newmanagement') }}</th>
            <th>{{ 'Ramais'|__('newmanagement') }}</th>
          </tr>
        </thead>
        <tbody>
          {% for item in items %}
          <tr>
            <td>{{ item.id }}</td>
            <td>
              <a href="{{ path('GlpiPlugin\\Newmanagement\\IpbxCloud') }}?id={{ item.id }}">
                {{ item.name }}
              </a>
            </td>
            <td>{{ item.company_name ?? '-' }}</td>
            <td>{{ item.provider ?? '-' }}</td>
            <td>{{ item.cloud_region ?? '-' }}</td>
            <td>{{ item.extensions_count ?? 0 }}</td>
          </tr>
          {% else %}
          <tr>
            <td colspan="6" class="text-center text-muted py-4">
              {{ 'Nenhum IPBX Cloud cadastrado.'|__('newmanagement') }}
            </td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
  </div>

</div>
{% endblock %}
